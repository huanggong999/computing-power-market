# 必填:后端 API 地址。前端会请求 /api/xxx,本进程把 /api 反代到 BACKEND_URL
BACKEND_URL=http://127.0.0.1:8082

# 监听端口,默认 5181(避开 ux-admin-main 默认的 5180)
PORT=5181

# 监听地址,默认 0.0.0.0,内网/外网都能访问。改成 127.0.0.1 表示只允许本机访问
HOST=0.0.0.0
